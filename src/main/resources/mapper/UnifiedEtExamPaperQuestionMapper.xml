<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.wmsiescore.mapper.EtExamPaperQuestionMapper">
    <resultMap id="BaseResultMap" type="com.example.wmsiescore.model.EtExamPaperQuestion">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="exam_paper_id" property="examPaperId" jdbcType="BIGINT"/>
        <result column="question_id" property="questionId" jdbcType="BIGINT"/>
        <result column="sort_order" property="sortOrder" jdbcType="INTEGER"/>
        <result column="score" property="score" jdbcType="INTEGER"/>
        <result column="status" property="status" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, exam_paper_id, question_id, sort_order, score, status, create_time, update_time
    </sql>

    <!-- 标准CRUD操作 -->
    
    <!-- 1. 带条件的新增数据 -->
    <insert id="insertSelective" parameterType="com.example.wmsiescore.model.EtExamPaperQuestion" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO et_exam_paper_question
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="examPaperId != null">exam_paper_id,</if>
            <if test="questionId != null">question_id,</if>
            <if test="sortOrder != null">sort_order,</if>
            <if test="score != null">score,</if>
            <if test="status != null">status,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
            <if test="examPaperId != null">#{examPaperId},</if>
            <if test="questionId != null">#{questionId},</if>
            <if test="sortOrder != null">#{sortOrder},</if>
            <if test="score != null">#{score},</if>
            <if test="status != null">#{status},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <!-- 2. 带条件的插入数据（插入或更新） -->
    <insert id="insertOrUpdate" parameterType="com.example.wmsiescore.model.EtExamPaperQuestion">
        INSERT INTO et_exam_paper_question (exam_paper_id, question_id, sort_order, score, status, create_time, update_time)
        VALUES (#{examPaperId}, #{questionId}, #{sortOrder}, #{score}, #{status}, #{createTime}, #{updateTime})
        ON DUPLICATE KEY UPDATE
        sort_order = VALUES(sort_order),
        score = VALUES(score),
        status = VALUES(status),
        update_time = VALUES(update_time)
    </insert>

    <!-- 3. 带条件的查询 -->
    <select id="selectByCondition" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM et_exam_paper_question
        <where>
            <if test="id != null">id = #{id}</if>
            <if test="examPaperId != null">AND exam_paper_id = #{examPaperId}</if>
            <if test="questionId != null">AND question_id = #{questionId}</if>
            <if test="sortOrder != null">AND sort_order = #{sortOrder}</if>
            <if test="score != null">AND score = #{score}</if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <!-- 4. 带条件的分页查询 -->
    <select id="selectByConditionWithPage" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM et_exam_paper_question
        <where>
            <if test="condition.id != null">id = #{condition.id}</if>
            <if test="condition.examPaperId != null">AND exam_paper_id = #{condition.examPaperId}</if>
            <if test="condition.questionId != null">AND question_id = #{condition.questionId}</if>
            <if test="condition.sortOrder != null">AND sort_order = #{condition.sortOrder}</if>
            <if test="condition.score != null">AND score = #{condition.score}</if>
            <if test="condition.status != null and condition.status != ''">
                AND status = #{condition.status}
            </if>
        </where>
        ORDER BY create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 5. 统计符合条件的记录数 -->
    <select id="countByCondition" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM et_exam_paper_question
        <where>
            <if test="id != null">id = #{id}</if>
            <if test="examPaperId != null">AND exam_paper_id = #{examPaperId}</if>
            <if test="questionId != null">AND question_id = #{questionId}</if>
            <if test="sortOrder != null">AND sort_order = #{sortOrder}</if>
            <if test="score != null">AND score = #{score}</if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
    </select>

    <!-- 6. 批量插入 -->
    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO et_exam_paper_question (exam_paper_id, question_id, sort_order, score, status, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.examPaperId}, #{item.questionId}, #{item.sortOrder}, #{item.score}, #{item.status}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <!-- 7. 批量删除 -->
    <delete id="batchDelete">
        DELETE FROM et_exam_paper_question WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 8. 带条件的更新 -->
    <update id="updateByCondition" parameterType="com.example.wmsiescore.model.EtExamPaperQuestion">
        UPDATE et_exam_paper_question
        <set>
            <if test="examPaperId != null">exam_paper_id = #{examPaperId},</if>
            <if test="questionId != null">question_id = #{questionId},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="score != null">score = #{score},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>

    <!-- 兼容原有方法 -->
    <insert id="insertExamPaperQuestion" parameterType="com.example.wmsiescore.model.EtExamPaperQuestion" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO et_exam_paper_question (exam_paper_id, question_id, sort_order, score, status, create_time)
        VALUES (#{examPaperId}, #{questionId}, #{sortOrder}, #{score}, #{status}, #{createTime})
    </insert>

    <update id="updateExamPaperQuestion" parameterType="com.example.wmsiescore.model.EtExamPaperQuestion">
        UPDATE et_exam_paper_question 
        SET sort_order = #{sortOrder}, 
            score = #{score}, 
            status = #{status}, 
            update_time = #{updateTime}
        WHERE id = #{id}
    </update>

    <delete id="deleteExamPaperQuestion">
        DELETE FROM et_exam_paper_question WHERE id = #{id}
    </delete>

    <delete id="deleteExamPaperQuestionByPaperAndQuestion">
        DELETE FROM et_exam_paper_question 
        WHERE exam_paper_id = #{examPaperId} AND question_id = #{questionId}
    </delete>

    <delete id="deleteQuestionsByExamPaper">
        DELETE FROM et_exam_paper_question WHERE exam_paper_id = #{examPaperId}
    </delete>

    <select id="getExamPaperQuestion" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM et_exam_paper_question 
        WHERE exam_paper_id = #{examPaperId} AND question_id = #{questionId}
    </select>

    <select id="listQuestionsByExamPaper" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM et_exam_paper_question 
        WHERE exam_paper_id = #{examPaperId} AND status = 'active'
        ORDER BY sort_order ASC, create_time ASC
    </select>

    <select id="listExamPapersByQuestion" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM et_exam_paper_question 
        WHERE question_id = #{questionId}
        ORDER BY create_time DESC
    </select>

    <update id="updateQuestionSortOrder">
        UPDATE et_exam_paper_question 
        SET sort_order = #{sortOrder} 
        WHERE id = #{id}
    </update>

    <update id="updateQuestionScore">
        UPDATE et_exam_paper_question 
        SET score = #{score} 
        WHERE id = #{id}
    </update>

    <update id="updateExamPaperQuestionStatus">
        UPDATE et_exam_paper_question 
        SET status = #{status} 
        WHERE id = #{id}
    </update>

    <select id="getMaxSortOrder" resultType="java.lang.Integer">
        SELECT COALESCE(MAX(sort_order), 0) 
        FROM et_exam_paper_question 
        WHERE exam_paper_id = #{examPaperId}
    </select>
</mapper>